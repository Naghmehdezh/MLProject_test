name: tutorial

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      yearly_seasonality: {type: int, default: 20}
      weekly_seasonality: {type: bool , default: False }
    command: "python train.py {yearly_seasonality} {weekly_seasonality}"